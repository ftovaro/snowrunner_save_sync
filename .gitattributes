# SnowRunner JSON save files - treat as binary to preserve null terminators
# Only these two files need the null terminator preserved
remote/CompleteSave.cfg -text
remote/CommonSslSave.cfg -text

# Binary map files - treat as binary
remote/fog_*.cfg binary
remote/sts_level_*.cfg binary
remote/sts_mudmaps_*.cfg binary
*.vdf binary
